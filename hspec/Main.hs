import BasePrelude
import Test.Hspec

main = 
  hspec $ do
    context "Head" $ do
      it "No module head" $ do
        pending
      it "No imports" $ do
        pending
    context "Errors" $ do
      it "Record syntax" $ do
        pending
      it "Nested HSE" $ do
        pending
      it "Outer HSE" $ do
        pending
